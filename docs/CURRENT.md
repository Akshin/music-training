# Текущий статус

Обновлено 2026-09-14.

Приложение — тренажёр для всех музыкантов, не для одного инструмента.
Репозиторий — `Akshin/music-training` (было `tetrachord--guitar-training`),
Pages — `https://akshin.github.io/music-training/`.

## Сделано

- Перенос `audio-core` в `src/` и удаление Tone.js-стека (`a5869a5`).
- Метроном тетрахордной тренировки на `audio-core` (`1cfe2f4`): BPM и размер
  меняются на лету (`TempoMap`, `WebTransport.retime`), щелчки трёх уровней,
  6/8 — акценты на 1 и 4, `TrainingStage` идёт по тактам аудиочасов.
- Референс (`6bf46dc`): переключатели «Метроном» и «Мелодия»; мелодия — гамма
  текущего лада восьмыми вверх-вниз от сильной доли цикла; тембр — мягкое
  FM-электропиано (`renderTone` в ядре).
- Бэк-треки (`080808a`): 12 mp3 струнной педали, блок «Звук» с тональным
  центром, «Мелодией» и «Бэк-треком»; тональный центр один на педаль, мелодию и
  подсказки; педаль играет `WebLoop` на общем `AudioContext`.
- Не закоммичено:
  - Лаборатория: `/lab` — песочница компонентов на живом звуке. Категории
    «Громкость» (`VolumeBar`, `VolumeSegments`, `VolumeCapsule`) и «Высота»
    (`PitchRoll`: гибкая ось нот — диапазон делит высоту графика, подписи редеют
    на тесных дорожках; в лабе переключатель от квинты до трёх октав и кнопка
    «Пустить ноты» — случайные цели для просмотра отображения; полоса цвета громкости, ноты вне
    диапазона — линия по краю и шеврон). Соглашение о цвете громкости —
    `src/components/loudness.ts`.
  - Каркас упражнения: `useExerciseSession` (метроном, подсказка, бэк-трек и
    микрофон на одном `AudioContext`, `traceTimeOf`, `scoreNotes`),
    `useExercise` (общие настройки + сессия), `ExerciseScreen` и
    `ExerciseConsole`, описания в `training/exercises.ts`. Тетрахорды и `/lab`
    переведены на каркас; `useTrainingSound` и `useMicSignal` удалены.
  - Выдвижная панель контролов `ControlSheet`: серая стрелка внизу по центру
    и свайп по ней открывают и закрывают панель; на десктопе она выезжает
    снизу вверх в потоке страницы, на экранах до 860 px — поверх страницы с
    затемнением и ручкой. `ExerciseConsole` построена на ней; в `/lab`
    категория «Контролы» и настоящая консоль внизу (`useExercise(SANDBOX)`).
  - Упражнение «Нота на долю» (`/note-on-beat`) удалено; его роль — микрофон,
    цели на `PitchRoll`, оценка высоты и времени — перешла к прогону
    тренировки из конструктора. `foldCents` переехал в `customRun.ts`.
  - Третье упражнение «Открываем рот» (`/mouth-opening`): вебка +
    MediaPipe Face Landmarker (`@mediapipe/tasks-vision`) в `useMouthTracker`
    — открытость и сужение рта в [0, 1]; `MouthFigure` (SVG) повторяет рот.
    Модель грузится с серверов Google, wasm собирается из пакета.
    В `/lab` — категория «Рот»: рот с камеры и ползунок `success`.
  - Тембр: экстрактор `harmonics` в ядре (H1–H3 в dBFS), `session.timbre`
    (усреднение за 80 мс), логика в `training/timbre.ts` (соотношения H2/H1 и
    H3/H1, `success` против цели H2 ≥ −8, H3 ≥ −12 дБ). `HarmonicStrings` —
    три струны, колеблются в модах 1–3 и сплетаются в кольцо при success = 1;
    в `/lab` — категория «Тембр». Проверено фейковым микрофоном в headless
    Chrome; цель подобрана на глаз — уточнить на живом голосе.
  - Конструктор тренировок — две страницы, как у тренировки:
    `/builder/trainings` (`ListView`) — список сохранённых с правкой, ссылкой и
    удалением; `/builder/edit` (`EditView`, `?id=` — правка сохранённой) —
    сам конструктор: условия (диапазон BPM и громкость — по желанию, смык,
    размер), ноты с клавиатуры-пианино (или клавиш A W S E D F T G Y H U J,
    Z/X — октава, R — пауза, Backspace — отмена, 1–5 — длительность) с
    длительностью и типом (обычная, атака, слайд в следующую ноту).
    Кроме нот и пауз — вдох (V) и выдох (B) со своей длительностью, без
    высоты: вуали дымки (`components/pitch/breath.ts`) — вдох собирает дымку
    вверх в точку у следующей ноты, выдох выпускает её из точки у предыдущей
    вниз; одно движение длится ровно столько, сколько дыхание при темпе. В
    предпросмотре и карточках анимация по кругу, в прогоне — по моменту «сейчас».
    Заполненный такт улетает карточкой в таймлайн, удаляется крестиком; внизу
    — предпросмотр в `PitchRoll`. Логика — `training/builder.ts`.
    Сохранение — `useCustomTrainings` (список в `localStorage`, у новой
    тренировки до сохранения — черновик). «Получить ссылку» —
    `{origin}{base}custom-training?d=…`: `training/customTraining.ts` пакует
    название, условия и полные такты в компактный JSON, deflate-raw и
    base64url (префикс `z`; без `CompressionStream` — `j`, без сжатия).
    Название и описание — поля в блоке «О тренировке»; описание уходит в
    ссылку и встречает попапом (`<dialog>`) на `/custom-training`, открывается
    снова кнопкой «Описание»; в списке — первые две строки.
    Реприза: в таймлайне «Реприза» → первый и последний такт блока, ×2…×8,
    знаки повтора на крайних карточках; повторы разворачиваются
    (`expandBars`) в предпросмотре, ссылке и прогоне.
  - Прогон тренировки по ссылке: `/custom-training` (`CustomTrainingView`
    декодирует `d`, `views/custom-training/CustomTrainingRun` — экран на
    каркасе упражнения). Такт отсчёта, дальше такты по кругу, каждый круг —
    проход; мелодия-подсказка (переключатель «Мелодия»), цели и вуали дыхания
    на `PitchRoll`, до Play — начало тренировки на графике. После каждого
    такта оценка нот: высота (±50 ¢, октава не важна), время (±120 мс),
    громкость по зоне тренировки (средний уровень кадров ноты); итог прохода.
    Темп в консоли ограничен диапазоном тренировки (`BpmControl` min/max),
    размер закреплён. Логика — `training/customRun.ts`, оценка —
    композабл `useTrainingScore` (такт → ноты → итог прохода). Проверено в headless
    Chrome с фейковым микрофоном (конвейер тактов, оценок и проходов);
    на живом голосе ещё не пели. Смык и дыхание не оцениваются — только
    подсказки.

## Следующий шаг

1. Спеть тренировку по ссылке (`/custom-training`) с настоящим микрофоном (в
   браузерной панели он заблокирован): попадание в центрах и миллисекундах,
   особенно в наушниках и на Bluetooth, слайды (оценивается начальная высота)
   и короткие ноты.
2. Калибровка задержки по записанному щелчку (движок умеет —
   `estimateLatency`), если оценка времени окажется смещённой.
3. Послушать в Chrome и Safari: тембр мелодии, баланс педали, мелодии и
   щелчков, шов на границе петли бэк-трека.
4. Вынести порядок ладов из `TrainingStage` в логику упражнения.
5. Тексты описания тетрахордов (`IssueView`) и иконки ещё говорят про гриф и
   гитару — переписать под всех музыкантов.

## Известные ограничения

- В фоновой вкладке браузер режет `setInterval` до ~1 Гц, щелчки пропадают.
- Пульс кнопки Play (`PlayTransport`) — отдельная CSS-анимация, с метрономом
  не синхронизирован.
- При смене размера во время игры первые ноты мелодии нового такта могут
  пропасть: план после смены приходит по кадрам отрисовки, позже планировщика.
- Бэк-трек — 5 МБ на тональность: при первом включении педаль вступает после
  загрузки и декодирования, не с первой доли.
- Время кадров микрофона сводится с сеткой по моменту старта захвата (точность
  до кванта рендера) и `outputLatency + baseLatency`; входная задержка
  микрофона не учитывается.
- В рабочей копии висит незакоммиченное удаление `tetr_scheme.png` и PDF
  «Выбор стека для аудиодвижка».

## Примечание

До переноса (коммит `24714ca`, 2026-09-09) движок `audio-core` жил в корне
репозитория и содержал только v1-функциональность (M1–M7): mic → worklet →
worker tape/OPFS → стенд `/lab` (с 2026-09-14 `/lab` — песочница компонентов).
За рамками v1 в бэклоге: аккорды, WASM, mp3-в-ядре.
