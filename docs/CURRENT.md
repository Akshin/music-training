# Текущий статус

Обновлено 2026-09-13.

Приложение — тренажёр для всех музыкантов, не для одного инструмента.
Репозиторий — `Akshin/music-training` (было `tetrachord--guitar-training`),
Pages — `https://akshin.github.io/music-training/`.

## Сделано

- Перенос `audio-core` в `src/` и удаление Tone.js-стека (`a5869a5`).
- Метроном тетрахордной тренировки на `audio-core` (`1cfe2f4`): BPM и размер
  меняются на лету (`TempoMap`, `WebTransport.retime`), щелчки трёх уровней,
  6/8 — акценты на 1 и 4, `TrainingStage` идёт по тактам аудиочасов.
- Референс:
  - переключатели «Метроном» (по умолчанию вкл) и «Мелодия» (выкл) на экране
    прогона;
  - мелодия — гамма текущего лада восьмыми вверх-вниз от сильной доли цикла
    (`training/melody.ts`); тоника C4, с табами гитары — E4, как на табах;
  - тембр — мягкое FM-электропиано (`renderTone` в ядре) вместо чистого синуса;
  - `TrainingStage` отдаёт план (текущий и следующий цикл) сразу при старте,
    транспорт получает ноты через `setNotes` и досылает опоздавшие без дублей;
  - если смена лада приходит, пока карточка ещё летит, карточка садится сразу —
    звук и карточки не расходятся.

## Следующий шаг

1. Послушать в Chrome и Safari: тембр мелодии, баланс мелодии и щелчков.
2. Тексты описания тренировки (`IssueView`) и иконки ещё говорят про гриф и
   гитару — переписать под всех музыкантов.
3. Педаль на тонике вместо удалённого mp3-бэк-трека.

## Известные ограничения

- В фоновой вкладке браузер режет `setInterval` до ~1 Гц, щелчки пропадают
  (так же в `/lab`).
- Пульс кнопки Play (`PlayTransport`) — отдельная CSS-анимация, с метрономом
  не синхронизирован.
- При смене размера во время игры первые ноты мелодии нового такта могут
  пропасть: план после смены приходит по кадрам отрисовки, позже планировщика.
- В рабочей копии висит незакоммиченное удаление `tetr_scheme.png` и PDF
  «Выбор стека для аудиодвижка».

## Примечание

До переноса (коммит `24714ca`, 2026-09-09) движок `audio-core` жил в корне
репозитория и содержал только v1-функциональность (M1–M7): mic → worklet →
worker tape/OPFS → `/lab`. За рамками v1 в бэклоге: аккорды, WASM,
mp3-в-ядре.
