# Supabase: Dashboard → Project Settings → API Keys.
# Only the publishable key (sb_publishable_…) belongs here; never the secret or service_role key.
# Copy to .env.local (git-ignored) and fill in. Without them the app runs on localStorage only.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
